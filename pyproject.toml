[project]
name = "daily-job-search-agent"
version = "0.1.0"
description = "On-demand AI job shortlist and learning signal agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
